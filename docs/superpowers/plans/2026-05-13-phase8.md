# Phase 8: 集成测试 + 移动端适配 + 发布准备 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans

**Goal:** 端到端流程测试、移动端触控/尺寸适配、性能优化、生产构建与部署配置。

**Tech Stack:** Phaser 3, TypeScript, Vite, Playwright (E2E)

**前置依赖:** Phase 7 全部完成

---

### Task 1: 端到端冒烟测试

**Files:**
- Create: `tests/e2e/smoke.test.ts`
- Modify: `package.json`（添加Playwright依赖和脚本）

- [ ] **Step 1: 安装Playwright**

```bash
npm install -D @playwright/test
npx playwright install chromium
```

在 `package.json` 添加：

```json
{
  "scripts": {
    "test:e2e": "playwright test"
  }
}
```

- [ ] **Step 2: 创建Playwright配置 playwright.config.ts**

```typescript
import { defineConfig } from '@playwright/test';

export default defineConfig({
  testDir: './tests/e2e',
  timeout: 30000,
  use: {
    baseURL: 'http://localhost:5173',
    viewport: { width: 390, height: 844 },
  },
  webServer: {
    command: 'npm run dev',
    port: 5173,
    reuseExistingServer: true,
  },
});
```

- [ ] **Step 3: 编写冒烟测试 tests/e2e/smoke.test.ts**

```typescript
import { test, expect } from '@playwright/test';

test.describe('Stock Life Simulator - Smoke Tests', () => {
  test('should load the game and show title screen', async ({ page }) => {
    await page.goto('/');
    // Phaser canvas should be present
    await expect(page.locator('canvas')).toBeVisible({ timeout: 10000 });
    // Wait for game to initialize
    await page.waitForTimeout(3000);
  });

  test('game container should fit viewport', async ({ page }) => {
    await page.goto('/');
    const container = page.locator('#game-container');
    await expect(container).toBeVisible();
    const box = await container.boundingBox();
    expect(box).not.toBeNull();
    expect(box!.width).toBeGreaterThan(0);
    expect(box!.height).toBeGreaterThan(0);
  });

  test('should not have console errors on load', async ({ page }) => {
    const errors: string[] = [];
    page.on('console', msg => {
      if (msg.type() === 'error') errors.push(msg.text());
    });
    await page.goto('/');
    await page.waitForTimeout(5000);
    // Filter out known non-critical warnings
    const critical = errors.filter(e => !e.includes('favicon'));
    expect(critical).toEqual([]);
  });
});
```

- [ ] **Step 4: 运行E2E测试**

```bash
npm run test:e2e
```

Expected: All PASS — 游戏加载、canvas渲染、无控制台报错。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "test: add Playwright E2E smoke tests"
```

---

### Task 2: 单元测试完整性检查

**Files:**
- 无新文件，运行并补充现有测试

- [ ] **Step 1: 运行全部单元测试**

```bash
npm run test -- --reporter=verbose
```

- [ ] **Step 2: 检查覆盖率**

```bash
npx vitest run --coverage
```

在 `vite.config.ts` 中添加覆盖率配置（如需）：

```typescript
/// <reference types="vitest" />
import { defineConfig } from 'vite';

export default defineConfig({
  test: {
    coverage: {
      provider: 'v8',
      include: ['src/managers/**', 'src/utils/**', 'src/data/**'],
    },
  },
});
```

- [ ] **Step 3: 确保核心模块覆盖率 > 70%**

关键测试文件清单：
- `src/managers/__tests__/StockManager.test.ts` ✓
- `src/managers/__tests__/GameStateManager.test.ts` ✓
- `src/managers/__tests__/SettlementManager.test.ts` ✓
- `src/managers/__tests__/PriceTickEngine.test.ts` ✓
- `src/managers/__tests__/SaveManager.test.ts` ✓
- `src/managers/__tests__/LifeEventManager.test.ts` ✓
- `src/managers/__tests__/InfoManager.test.ts` ✓
- `src/utils/__tests__/moodCalculator.test.ts` ✓
- `src/utils/__tests__/marketTrend.test.ts` ✓
- `src/utils/__tests__/biasDetector.test.ts` ✓
- `src/utils/__tests__/scoring.test.ts` ✓
- `src/data/__tests__/scenes.test.ts` ✓

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "test: verify full test suite and coverage"
```

---

### Task 3: 移动端触控适配

**Files:**
- Modify: `src/config/gameConfig.ts`
- Modify: `index.html`

- [ ] **Step 1: 优化Phaser缩放配置**

```typescript
export const gameConfig: Phaser.Types.Core.GameConfig = {
  type: Phaser.AUTO,
  parent: 'game-container',
  width: GAME_WIDTH,
  height: GAME_HEIGHT,
  backgroundColor: '#12101f',
  scale: {
    mode: Phaser.Scale.FIT,
    autoCenter: Phaser.Scale.CENTER_BOTH,
    min: { width: 320, height: 568 },
    max: { width: 430, height: 932 },
  },
  input: {
    touch: true,
    activePointers: 2,
  },
  scene: [/* all scenes */],
};
```

- [ ] **Step 2: 增强index.html的移动端meta**

```html
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, viewport-fit=cover" />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
<meta name="theme-color" content="#12101f" />
<meta name="description" content="股票人生模拟器 — 一款以真实A股历史数据为基底的人生模拟+股票交易策略游戏" />
```

添加防止双击缩放和pull-to-refresh的CSS：

```html
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  html, body {
    background: #12101f;
    overflow: hidden;
    position: fixed;
    width: 100%;
    height: 100%;
    touch-action: none;
    -webkit-overflow-scrolling: none;
    overscroll-behavior: none;
  }
  #game-container {
    width: 100vw;
    height: 100vh;
    height: 100dvh; /* dynamic viewport height for mobile */
  }
</style>
```

- [ ] **Step 3: 验证移动端模拟**

用浏览器DevTools切换到手机模式（iPhone 14 Pro、Pixel 7等），测试：
- 游戏画面正确缩放居中
- 触摸滚动自选股列表正常
- 按钮点击响应正常
- 无页面弹跳/误触缩放

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: optimize mobile touch and viewport handling"
```

---

### Task 4: 性能优化

**Files:**
- Modify: `src/scenes/TradingScene.ts`
- Modify: `vite.config.ts`

- [ ] **Step 1: 对象池优化（StockRow复用）**

在 `StockListPanel` 中，复用已创建的StockRow对象而非每次重建：

```typescript
// StockRow 已经实现了 updatePrice 方法
// 确保只在初始化时创建一次，后续只调用 updatePrice
// 检查是否有不必要的 destroy/recreate 操作
```

- [ ] **Step 2: 减少Graphics重绘**

在 `TimelineChart` 和 `CandlestickChart` 中，仅在数据变化时重绘：

```typescript
private lastDrawnLength = 0;

addTick(price: number): void {
  this.prices.push(price);
  // 仅在新tick时重绘
  if (this.prices.length !== this.lastDrawnLength) {
    this.redraw();
    this.lastDrawnLength = this.prices.length;
  }
}
```

- [ ] **Step 3: Vite生产构建优化**

```typescript
// vite.config.ts
import { defineConfig } from 'vite';

export default defineConfig({
  build: {
    target: 'es2020',
    minify: 'terser',
    terserOptions: {
      compress: { drop_console: true },
    },
    rollupOptions: {
      output: {
        manualChunks: {
          phaser: ['phaser'],
        },
      },
    },
    chunkSizeWarningLimit: 1500,
  },
  test: {
    coverage: {
      provider: 'v8',
      include: ['src/managers/**', 'src/utils/**', 'src/data/**'],
    },
  },
});
```

- [ ] **Step 4: 构建并检查产物大小**

```bash
npm run build
ls -lh dist/assets/
```

Expected: Phaser chunk ~1.2MB (gzip ~400KB)，业务代码 < 100KB。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "perf: optimize rendering and production build"
```

---

### Task 5: PWA基础配置（可选）

**Files:**
- Create: `public/manifest.json`
- Modify: `index.html`

- [ ] **Step 1: 创建 public/manifest.json**

```json
{
  "name": "股票人生模拟器",
  "short_name": "股票人生",
  "description": "以真实A股历史数据为基底的人生模拟+股票交易策略游戏",
  "start_url": "/",
  "display": "standalone",
  "orientation": "portrait",
  "background_color": "#12101f",
  "theme_color": "#12101f",
  "icons": [
    {
      "src": "/icon-192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/icon-512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ]
}
```

- [ ] **Step 2: 在index.html中引用manifest**

```html
<link rel="manifest" href="/manifest.json" />
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: add PWA manifest for mobile install"
```

---

### Task 6: 部署配置

**Files:**
- Create: `deploy.sh`
- Create: `.github/workflows/deploy.yml`（可选）

- [ ] **Step 1: 创建部署脚本 deploy.sh**

```bash
#!/bin/bash
set -e

echo "🔨 Building production bundle..."
npm run build

echo "📦 Build complete!"
echo "   Output: ./dist"
echo ""
echo "Deploy options:"
echo "  1. GitHub Pages: push dist/ to gh-pages branch"
echo "  2. Vercel: npx vercel ./dist"
echo "  3. Netlify: drag dist/ to netlify.com"
echo "  4. 静态服务器: npx serve dist"

echo ""
echo "🧪 Quick local preview:"
echo "   npx serve dist"
```

- [ ] **Step 2: 创建GitHub Pages部署配置（可选）**

```yaml
# .github/workflows/deploy.yml
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run test
      - run: npm run build
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - uses: actions/deploy-pages@v4
```

- [ ] **Step 3: 本地预览验证**

```bash
npm run build
npx serve dist
```

Expected: 在 http://localhost:3000 打开，完整游戏可正常运行。

- [ ] **Step 4: Commit**

```bash
chmod +x deploy.sh
git add -A
git commit -m "chore: add deployment scripts and CI config"
```

---

### Task 7: README文档

**Files:**
- Create: `README.md`

- [ ] **Step 1: 编写 README.md**

```markdown
# 🎮 股票人生模拟器 (Stock Life Simulator)

> 所有的选择都有价格。

一款以真实A股历史数据为基底的**人生模拟+股票交易策略游戏**。

## 🎯 游戏特色

- 📈 **真实行情体验**：基于A股历史数据，价格实时步进跳动
- 🌙 **场景式社交**：盘后选择去酒吧、大排档还是回家，每个选择都有价格
- 🧠 **认知偏差诊断**：追涨杀跌？损失厌恶？游戏结束后帮你照镜子
- ⭐ **四维评分**：投资智慧 / 社交回报 / 生活平衡 / 心态稳定

## 🚀 快速开始

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 运行测试
npm run test

# 生产构建
npm run build
```

## 🛠 技术栈

- **游戏引擎**: Phaser 3
- **语言**: TypeScript
- **构建**: Vite
- **测试**: Vitest + Playwright

## 📁 项目结构

```
src/
├── config/       # 游戏配置、常量
├── data/         # 角色、场景、行情数据
├── managers/     # 游戏状态、股票数据、存档管理
├── models/       # TypeScript类型定义
├── scenes/       # Phaser场景（盘前/盘中/盘后/结算/复盘）
├── ui/           # UI组件（图表、卡片、弹窗、主题）
└── utils/        # 工具函数（心情计算、偏差检测、评分）
```

## 📄 License

MIT
```

- [ ] **Step 2: Commit**

```bash
git add -A
git commit -m "docs: add comprehensive README"
```

---

### Task 8: 最终验证清单

- [ ] **Step 1: 完整流程走通测试**

手动走一遍完整流程：
1. 启动游戏 → 新游戏
2. 盘前：查看大盘简报、推荐股、角色信息
3. 盘中：观察价格跳动、买入一只股票、查看持仓Tab
4. 点击股票进入详情 → 分时图/K线切换
5. 收盘 → 盘后场景选择
6. 事件卡片选择 → 汇总
7. 结算：检查日薪/成本/收益率
8. 重复3天
9. 触发一次生活突发事件
10. 游戏结束 → 复盘4步流程
11. 再来一局

- [ ] **Step 2: 运行全部测试**

```bash
npm run test
npm run test:e2e
```

Expected: All PASS.

- [ ] **Step 3: 生产构建无报错**

```bash
npm run build
```

Expected: 无TypeScript错误，无构建警告。

- [ ] **Step 4: 最终Commit + Tag**

```bash
git add -A
git commit -m "chore: Phase 1 MVP complete"
git tag v0.1.0 -m "Phase 1 MVP - Core gameplay loop"
```
