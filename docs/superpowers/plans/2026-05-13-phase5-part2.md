# Phase 5 Part 2: 信息系统集成 + InfoManager完善 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans

**Goal:** 将InfoManager完整集成到GameManager中，实现三层信息系统的前两层（免费信息+场景付费信息），信息收集面板UI，情报串联提示。

**前置依赖:** Phase 5 Part 1 完成

---

### Task 5: InfoManager集成到GameManager

**Files:**
- Modify: `src/managers/GameManager.ts`
- Modify: `src/managers/InfoManager.ts`

- [ ] **Step 1: 更新GameManager添加InfoManager**

```typescript
import Phaser from 'phaser';
import { GameStateManager } from './GameStateManager';
import { StockManager } from './StockManager';
import { InfoManager } from './InfoManager';
import { getCharacter } from '../data/characters';
import sampleMarket from '../data/sampleMarket.json';

export class GameManager {
  public state: GameStateManager;
  public stocks: StockManager;
  public info: InfoManager;

  constructor() {
    const char = getCharacter('programmer');
    this.state = new GameStateManager(char.id, char.startingCash, sampleMarket.totalDays);
    this.stocks = new StockManager(sampleMarket as any);
    this.info = new InfoManager(this.stocks.getStockIds());
  }

  getCommissionRate(): number {
    const char = getCharacter(this.state.getState().characterId);
    const s = this.state.getState();
    return s.commissionDiscountDays > 0
      ? char.commissionRate / 2
      : char.commissionRate;
  }
}

export function getGameManager(scene: Phaser.Scene): GameManager {
  return scene.registry.get('gameManager') as GameManager;
}
```

- [ ] **Step 2: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: integrate InfoManager into GameManager"
```

---

### Task 6: 场景事件中的信息收集

**Files:**
- Modify: `src/scenes/PostMarketScene.ts`

- [ ] **Step 1: 在事件完成回调中记录信息**

修改 `PostMarketScene.showNextEvent()` 中的事件完成回调：

```typescript
// 在 EventCard onComplete 回调中，替换现有的简化注释
new EventCard(this, GAME_WIDTH / 2, 90, GAME_WIDTH - 40, event, (result) => {
  this.eventResults.push(result);
  this.totalSpending += result.cost;

  // 记录获得的信息
  if (result.gotInfo) {
    const gm = getGameManager(this);
    const state = gm.state.getState();
    const info = gm.info.generateInfo(
      this.selectedScene!.id,
      result.infoAccuracy,
      state.currentDay,
    );
    if (info) {
      gm.info.addInfo(info);
      // 检查情报串联
      if (gm.info.checkChain(info.stockId)) {
        result.message += ` 🔗 情报串联触发！${info.stockId} 已获多源验证`;
      }
    }
  }

  this.time.delayedCall(300, () => this.showNextEvent());
});
```

- [ ] **Step 2: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: record info from scene events into InfoManager"
```

---

### Task 7: 信息收集面板

**Files:**
- Create: `src/ui/InfoPanel.ts`

- [ ] **Step 1: 创建 src/ui/InfoPanel.ts**

在盘前场景中可查看已收集的所有信息：

```typescript
import Phaser from 'phaser';
import { InfoItem } from '../models/types';

export class InfoPanel extends Phaser.GameObjects.Container {
  constructor(
    scene: Phaser.Scene,
    x: number, y: number,
    width: number, height: number,
    infoList: InfoItem[],
    chainedStocks: string[],
    onClose: () => void,
  ) {
    super(scene, x, y);
    scene.add.existing(this);

    // 背景
    const bg = scene.add.rectangle(0, 0, width, height, 0x1a1a2e, 0.98).setOrigin(0, 0);
    this.add(bg);

    // 标题
    this.add(scene.add.text(width / 2, 20, '📋 情报收集', {
      fontSize: '18px', color: '#ffd700', fontFamily: 'sans-serif',
    }).setOrigin(0.5));

    // 关闭按钮
    const closeBtn = scene.add.text(width - 16, 16, '✕', {
      fontSize: '20px', color: '#888', fontFamily: 'sans-serif',
    }).setOrigin(1, 0).setInteractive({ useHandCursor: true });
    closeBtn.on('pointerup', onClose);
    this.add(closeBtn);

    if (infoList.length === 0) {
      this.add(scene.add.text(width / 2, height / 2, '暂无情报\n通过盘后场景获取', {
        fontSize: '16px', color: '#555', fontFamily: 'sans-serif',
        align: 'center',
      }).setOrigin(0.5));
      return;
    }

    // 按股票分组
    const grouped: Record<string, InfoItem[]> = {};
    for (const info of infoList) {
      if (!grouped[info.stockId]) grouped[info.stockId] = [];
      grouped[info.stockId].push(info);
    }

    let rowY = 56;
    for (const [stockId, items] of Object.entries(grouped)) {
      const isChained = chainedStocks.includes(stockId);
      const headerColor = isChained ? '#ffd700' : '#4a90d9';
      const chainLabel = isChained ? ' 🔗 已串联' : '';

      this.add(scene.add.text(16, rowY, `${stockId}${chainLabel}`, {
        fontSize: '15px', color: headerColor, fontFamily: 'sans-serif',
      }));
      rowY += 24;

      for (const info of items) {
        const accColor = info.accuracy >= 0.7 ? '#2ecc71' :
                         info.accuracy >= 0.5 ? '#f39c12' : '#e74c3c';
        this.add(scene.add.text(28, rowY, `Day${info.day} [${info.source}]`, {
          fontSize: '11px', color: '#888', fontFamily: 'sans-serif',
        }));
        this.add(scene.add.text(width - 16, rowY, `${Math.round(info.accuracy * 100)}%`, {
          fontSize: '11px', color: accColor, fontFamily: 'monospace',
        }).setOrigin(1, 0));
        rowY += 18;

        this.add(scene.add.text(28, rowY, info.content, {
          fontSize: '13px', color: '#ccc', fontFamily: 'sans-serif',
          wordWrap: { width: width - 56 },
        }));
        rowY += 24;
      }
      rowY += 8;
    }
  }
}
```

- [ ] **Step 2: 在PreMarketScene添加情报查看入口**

在 `PreMarketScene.create()` 中添加情报按钮：

```typescript
// 情报按钮（右上角）
const infoBtnBg = this.add.rectangle(GAME_WIDTH - 50, 30, 32, 32, 0x333366, 0.8)
  .setInteractive({ useHandCursor: true });
this.add.text(GAME_WIDTH - 50, 30, '📋', { fontSize: '18px' }).setOrigin(0.5);
let infoPanel: InfoPanel | null = null;
infoBtnBg.on('pointerup', () => {
  if (infoPanel) {
    infoPanel.destroy();
    infoPanel = null;
    return;
  }
  const gm = getGameManager(this);
  const allInfo = gm.info.getAllInfo();
  const stockIds = gm.stocks.getStockIds();
  const chainedStocks = stockIds.filter(id => gm.info.checkChain(id));
  infoPanel = new InfoPanel(this, 0, 0, GAME_WIDTH, GAME_HEIGHT, allInfo, chainedStocks, () => {
    infoPanel?.destroy();
    infoPanel = null;
  });
});
```

- [ ] **Step 3: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add -A
git commit -m "feat: add InfoPanel and integrate into PreMarketScene"
```

---

### Task 8: 角色能力信息注入

**Files:**
- Modify: `src/scenes/PreMarketScene.ts`

- [ ] **Step 1: 将角色能力的信息存入InfoManager**

在 PreMarketScene 中角色能力部分，将信息实际存入：

```typescript
// 替换原来的 TODO 注释
if (ability.info) {
  gm.info.addInfo(ability.info);
}
```

- [ ] **Step 2: 运行全部测试**

```bash
npm run test
```

Expected: All PASS.

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: persist character ability info into InfoManager"
```
