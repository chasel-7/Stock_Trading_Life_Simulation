# Phase 7 Part 2: UI重构 + 微动画 + 音效 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development or superpowers:executing-plans

**Goal:** 用设计系统重构所有场景UI、添加微交互动画、集成音效系统。

**前置依赖:** Phase 7 Part 1 完成

---

### Task 6: BootScene视觉升级

**Files:**
- Modify: `src/scenes/BootScene.ts`

- [ ] **Step 1: 重写BootScene使用设计系统**

```typescript
import Phaser from 'phaser';
import { THEME } from '../ui/theme';
import { CardFactory } from '../ui/CardFactory';
import { Transition } from '../ui/Transition';
import { SaveManager } from '../managers/SaveManager';
import { GAME_WIDTH, GAME_HEIGHT } from '../config/gameConfig';

export class BootScene extends Phaser.Scene {
  constructor() {
    super({ key: 'BootScene' });
  }

  create(): void {
    // 背景渐变（用多层矩形模拟）
    this.add.rectangle(0, 0, GAME_WIDTH, GAME_HEIGHT, THEME.colors.bgPrimary, 1).setOrigin(0, 0);
    const glow = this.add.graphics();
    glow.fillStyle(THEME.colors.accentGlow, 0.05);
    glow.fillCircle(GAME_WIDTH / 2, GAME_HEIGHT / 3, 200);

    // 标题动画
    const title = this.add.text(GAME_WIDTH / 2, GAME_HEIGHT / 3 - 20, '股票人生模拟器', {
      fontSize: '30px', color: THEME.colors.textPrimary,
      fontFamily: THEME.font.primary,
    }).setOrigin(0.5).setAlpha(0);

    const subtitle = this.add.text(GAME_WIDTH / 2, GAME_HEIGHT / 3 + 24, '所有的选择都有价格', {
      fontSize: '14px', color: THEME.colors.textSecondary,
      fontFamily: THEME.font.primary,
    }).setOrigin(0.5).setAlpha(0);

    // 动画序列
    this.tweens.add({
      targets: title, alpha: 1, y: GAME_HEIGHT / 3, duration: 800,
      ease: 'Power2', delay: 200,
    });
    this.tweens.add({
      targets: subtitle, alpha: 1, duration: 600, delay: 700,
    });

    // 按钮
    const newBtn = CardFactory.createButton(
      this, GAME_WIDTH / 2, GAME_HEIGHT / 2 + 60, 240, 48,
      '🎮 新游戏', {
        onClick: () => {
          SaveManager.deleteSave();
          Transition.fadeToScene(this, 'PreMarketScene');
        },
      },
    ).setAlpha(0);
    this.tweens.add({ targets: newBtn, alpha: 1, delay: 1000, duration: 400 });

    if (SaveManager.hasSave()) {
      const contBtn = CardFactory.createButton(
        this, GAME_WIDTH / 2, GAME_HEIGHT / 2 + 120, 240, 48,
        '📂 继续游戏', {
          color: THEME.colors.positive,
          onClick: () => Transition.fadeToScene(this, 'PreMarketScene'),
        },
      ).setAlpha(0);
      this.tweens.add({ targets: contBtn, alpha: 1, delay: 1200, duration: 400 });
    }

    // 版本号
    this.add.text(GAME_WIDTH / 2, GAME_HEIGHT - 24, 'v1.0 · Phase 1', {
      fontSize: '11px', color: THEME.colors.textMuted,
      fontFamily: THEME.font.primary,
    }).setOrigin(0.5);
  }
}
```

- [ ] **Step 2: 验证编译**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add -A
git commit -m "feat: upgrade BootScene with theme design system"
```

---

### Task 7: 全场景UI主题适配

**Files:**
- Modify: `src/scenes/PreMarketScene.ts`
- Modify: `src/scenes/PostMarketScene.ts`
- Modify: `src/scenes/SettlementScene.ts`

- [ ] **Step 1: 批量替换硬编码颜色**

在所有场景中进行以下替换（全局搜索替换）：

```
0x1a1a2e → THEME.colors.bgPrimary
0x1e1e3a → THEME.colors.bgCard
0x16213e → THEME.colors.bgSecondary
0x333366 → THEME.colors.border
0x4a90d9 → THEME.colors.accent
'#e0e0e0' → THEME.colors.textPrimary
'#888' → THEME.colors.textSecondary
'#555' / '#aaa' → THEME.colors.textMuted
'#e74c3c' → THEME.colors.rise
'#2ecc71' → THEME.colors.fall
'#ffd700' → '#ffd700'（保持金色不变）
'sans-serif' → THEME.font.primary
'monospace' → THEME.font.mono
```

每个场景文件头部添加 `import { THEME } from '../ui/theme';`

- [ ] **Step 2: 将卡片替换为CardFactory**

将各场景中 `scene.add.rectangle(...).setStrokeStyle(...)` 的卡片样式替换为 `CardFactory.create()`，获得统一圆角效果。

- [ ] **Step 3: 将按钮替换为CardFactory.createButton()**

替换各场景中手动创建的按钮（rectangle + text）为 `CardFactory.createButton()`。

- [ ] **Step 4: 验证所有场景视觉统一**

```bash
npm run dev
```

Expected: 所有页面使用统一的深紫色调+圆角卡片风格，按钮有hover缩放效果。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: apply unified theme across all scenes"
```

---

### Task 8: 音效系统

**Files:**
- Create: `src/managers/AudioManager.ts`
- Create: `public/audio/` 目录下放置音效文件

- [ ] **Step 1: 创建 src/managers/AudioManager.ts**

使用 Web Audio API 生成程序化音效（无需外部音频文件）：

```typescript
export class AudioManager {
  private ctx: AudioContext | null = null;
  private enabled = true;

  init(): void {
    try {
      this.ctx = new AudioContext();
    } catch {
      this.enabled = false;
    }
  }

  toggle(): void {
    this.enabled = !this.enabled;
  }

  /** 按钮点击音 */
  playClick(): void {
    this.playTone(800, 0.05, 'sine');
  }

  /** 买入音效 */
  playBuy(): void {
    this.playTone(523, 0.1, 'sine');
    setTimeout(() => this.playTone(659, 0.1, 'sine'), 100);
  }

  /** 卖出音效 */
  playSell(): void {
    this.playTone(659, 0.1, 'sine');
    setTimeout(() => this.playTone(523, 0.1, 'sine'), 100);
  }

  /** 价格跳动音（微弱） */
  playTick(): void {
    this.playTone(1200, 0.02, 'sine', 0.05);
  }

  /** 好消息 */
  playGood(): void {
    this.playTone(523, 0.1, 'sine');
    setTimeout(() => this.playTone(659, 0.1, 'sine'), 120);
    setTimeout(() => this.playTone(784, 0.15, 'sine'), 240);
  }

  /** 坏消息 */
  playBad(): void {
    this.playTone(400, 0.15, 'sawtooth', 0.15);
    setTimeout(() => this.playTone(300, 0.2, 'sawtooth', 0.12), 150);
  }

  /** 日结算 */
  playSettle(): void {
    this.playTone(440, 0.15, 'triangle');
    setTimeout(() => this.playTone(554, 0.15, 'triangle'), 200);
    setTimeout(() => this.playTone(659, 0.2, 'triangle'), 400);
  }

  private playTone(
    freq: number,
    duration: number,
    type: OscillatorType = 'sine',
    volume: number = 0.1,
  ): void {
    if (!this.enabled || !this.ctx) return;

    const osc = this.ctx.createOscillator();
    const gain = this.ctx.createGain();
    osc.connect(gain);
    gain.connect(this.ctx.destination);

    osc.type = type;
    osc.frequency.value = freq;
    gain.gain.value = volume;
    gain.gain.exponentialRampToValueAtTime(0.001, this.ctx.currentTime + duration);

    osc.start();
    osc.stop(this.ctx.currentTime + duration);
  }
}

/** 全局单例 */
export const audioManager = new AudioManager();
```

- [ ] **Step 2: 在main.ts中初始化AudioManager**

```typescript
import { audioManager } from './managers/AudioManager';

// 在用户首次交互时初始化（浏览器策略要求）
document.addEventListener('pointerdown', () => {
  audioManager.init();
}, { once: true });
```

- [ ] **Step 3: 在关键交互点添加音效调用**

- `CardFactory.createButton`: 点击时 `audioManager.playClick()`
- `OrderDialog.confirm()`: 买入 `audioManager.playBuy()` / 卖出 `audioManager.playSell()`
- `PriceTickEngine.step()`: `audioManager.playTick()`
- `SettlementScene`: 进入时 `audioManager.playSettle()`
- `LifeEventPopup`: 出现时 `audioManager.playBad()`

- [ ] **Step 4: 验证编译和音效播放**

```bash
npx tsc --noEmit
npm run dev
```

Expected: 点击按钮、买卖、价格跳动、结算时都有轻微音效反馈。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: add procedural audio effects for key interactions"
```

---

### Task 9: 微交互动画打磨

**Files:**
- 各UI组件文件

- [ ] **Step 1: EventCard入场/退场动画增强**

在 `EventCard` 构造函数中增强入场动画：

```typescript
// 替换现有入场动画
this.setAlpha(0);
this.setScale(0.85);
this.y += 20;
scene.tweens.add({
  targets: this,
  alpha: 1, scale: 1, y: this.y - 20,
  duration: 400,
  ease: 'Back.easeOut',
});
```

- [ ] **Step 2: 结算数字滚动动画**

在 `SettlementScene` 的金额显示处使用 `Transition.countUp()`：

```typescript
const cashTxt = this.add.text(...);
Transition.countUp(this, cashTxt, 0, result.cashAfter, 1000);
```

- [ ] **Step 3: 场景选择卡片hover效果**

在 `PostMarketScene` 场景选择卡片的 hover 中添加：

```typescript
cardBg.on('pointerover', () => {
  this.tweens.add({ targets: cardBg, scaleX: 1.02, scaleY: 1.02, duration: 150 });
});
cardBg.on('pointerout', () => {
  this.tweens.add({ targets: cardBg, scaleX: 1, scaleY: 1, duration: 150 });
});
```

- [ ] **Step 4: 验证所有动画效果**

```bash
npm run dev
```

Expected: 卡片hover有微缩放、数字有滚动动画、事件卡片有弹性出入。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "feat: polish micro-interactions across all UI components"
```
